pour cloner un repository il faut entrer la commande suivante: git clone url du lien a cloner
Les 3 commandes du push sont les suivantes 
- git add
-git commit -m
ainsi que git push 
pour effectuer la commande pull il faut faire git pull

